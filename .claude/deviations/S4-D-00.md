# S4-D-00 — infra preflight: BLOCKED, not attempted-and-failed

**Date:** 2026-08-15
**Status:** Blocked on credentials and host access. No part of the ticket's checklist
was executed. Nothing below is a substitute for the recorded `free -g` / `df -h`
output or the working API call the ticket asks for — those do not exist yet.

## What the ticket requires

S4-D-00 asks for four things, all of which must run **from the target host**:

1. Confirm OpenAI Responses API access and the account's rate tier.
2. Record `free -g` / `df -h` from the target host and confirm ≥ 6 GB free headroom
   beyond existing services.
3. If headroom is insufficient, provision a second Hetzner box via API.
4. Confirm egress from the host to `api.openai.com`.

## What the orchestration environment actually has

Verified 2026-08-15 in the sprint-orchestration session (an isolated cloud container,
**not** the target host):

- No `OPENAI_API_KEY` (or any OpenAI credential) in the environment.
- No Hetzner API token (`HCLOUD_TOKEN` or otherwise).
- No Namecheap API key, no Coolify token, no OpenClinica admin credentials.
- No SSH keys (`~/.ssh` is empty) and no record of the target host's address —
  there is no way to reach the host at all, let alone run checks on it.
- The container's own egress goes through a policy-scoped proxy; its reachability
  proves nothing about the target host and is deliberately **not** recorded here as
  evidence.

## Consequence for the dependency chain

S4-D-00 gates the hard sequence S4-D-10 → S4-D-11 → S4-D-12. All three are therefore
blocked, and with them everything that depends on them: S4-D-20 (needs S4-D-11's
OpenClinica), S4-D-21 (needs S4-D-10/11), S4-D-40 (needs S4-D-12), and S4-D-41
(needs everything). None of these were started; per the sprint's execution rules the
gate is reported rather than worked around, and no data was invented to proceed.

S4-D-13 → S4-D-30 (the Lean AuditBound lane, pure code) is independent of this chain
and proceeded separately.

## To unblock

Provide to the orchestration environment (or run S4-D-00 manually from the host):

1. SSH access to the target host (address + key), or the host's recorded
   `free -g` / `df -h` output.
2. `OPENAI_API_KEY` with Responses API access, so the access/rate-tier check is a
   real API call.
3. A Hetzner API token — only needed if the headroom check fails and a second box
   must be provisioned.
4. For the tickets immediately downstream: Namecheap API key (S4-D-10 DNS), Coolify
   token (S4-D-10 deploy), and the OpenClinica admin credentials once S4-D-11
   creates them.

Every credential above belongs in the environment, never in a tracked file (§2B.3).
